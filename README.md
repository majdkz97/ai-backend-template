# AI Backend Template

Reusable FastAPI + PostgreSQL + Docker Compose template for AI projects (RAG, agents, multimodal apps).

## Features
- FastAPI with Pydantic & SQLModel
- PostgreSQL persistence
- Docker & Docker Compose
- Environment variables
- Basic auth & logging (to be added)

## Setup (local)

1. Clone repo
2. `cp .env.example .env`
3. `docker compose up --build`

## Deployment
- Render.com (Docker Compose support)
- Railway.app / Fly.io

## Tech Stack
- FastAPI
- SQLModel
- PostgreSQL
- Docker Compose

Built by Majd Kazmoz